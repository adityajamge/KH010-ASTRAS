# Copy to .env.development / .env for local development
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=JalSetu
# Publishable key from the Clerk dashboard (https://dashboard.clerk.com).
# Required — the app shows a setup notice until this is set.
VITE_CLERK_PUBLISHABLE_KEY=
